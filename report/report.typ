// Лабораторная работа № 1
// Титульный лист отделён от шаблона modern-g7-32,
// чтобы номер страницы 1 не отображался.

#import "@preview/modern-g7-32:0.2.0": gost

// -----------------------------------------------------------------------------
// ТИТУЛЬНЫЙ ЛИСТ
// -----------------------------------------------------------------------------

// Титульный лист создаётся ДО подключения шаблона modern-g7-32.
// Поэтому пакет не может вывести на нём номер страницы.
#set page(
  paper: "a4",
  margin: (left: 30mm, right: 15mm, top: 20mm, bottom: 20mm),
  numbering: none,
  footer: none,
)

#set text(font: "Times New Roman", size: 14pt, lang: "ru")

#block[
#set par(first-line-indent: 0pt, justify: false)

#grid(
  columns: (1fr,),
  rows: (auto, 1fr, auto, 1fr, auto, 1fr, auto),
  row-gutter: 0pt,

  align(center)[
    #text(weight: "bold")[МОСКОВСКИЙ АВИАЦИОННЫЙ ИНСТИТУТ] \
    #text(weight: "bold")[(НАЦИОНАЛЬНЫЙ ИССЛЕДОВАТЕЛЬСКИЙ УНИВЕРСИТЕТ)]
    #v(6mm)
    Институт № 8 «Компьютерные науки»
  ],

  [],

  align(center)[
    #text(weight: "bold")[ЛАБОРАТОРНАЯ РАБОТА № 1] \
    по курсу «Программирование графических процессоров»
    #v(7mm)
    #text(weight: "bold")[
      «Освоение программного обеспечения для работы с технологией CUDA. \
      Примитивные операции над векторами»
    ]
  ],

  [],

  align(right)[
    #block(width: 92mm)[
      #table(
        columns: (40mm, 1fr),
        stroke: none,
        inset: (x: 0pt, y: 3pt),
        align: (left, left),
        [Выполнил:], [Д. А. Кулешов],
        [Группа:], [М8О-408Б-23],
        [Преподаватель:], [А. Ю. Морозов],
      )
    ]
  ],

  [],

  align(center)[Москва 2026],
)
]

#pagebreak()


// -----------------------------------------------------------------------------
// С ЭТОГО МЕСТА РАБОТАЕТ modern-g7-32
// -----------------------------------------------------------------------------

#show: gost.with(
  hide-title: true,
  text-size: (default: 14pt, small: 12pt),
  indent: 1.25cm,
  margin: (
    left: 30mm,
    right: 15mm,
    top: 20mm,
    bottom: 20mm,
  ),
  pagination-align: center,
  add-pagebreaks: false,
)

#set text(font: "Times New Roman", lang: "ru")
#show raw: set text(font: "Courier New", size: 12pt)

#let cuda-config(blocks, threads) = raw(
  "<<<" + str(blocks) + ", " + str(threads) + ">>>"
)

// -----------------------------------------------------------------------------
// ОСНОВНАЯ ЧАСТЬ
// -----------------------------------------------------------------------------

= Условие

Цель работы: освоить основные средства CUDA и научиться выполнять простые
операции над векторами на графическом процессоре.

Условие задания представлено на рисунке 1.

#figure(
  image("task.png", width: 90%),
  caption: [Условие задания],
) <task-figure>

= Программное и аппаратное обеспечение

== Аппаратное обеспечение

Характеристики использованного аппаратного обеспечения приведены в таблице 1.

// Таблицы оставлены разбиваемыми штатными средствами modern-g7-32.
// table.header(...) повторяет шапку при переносе на следующую страницу.
// Для очень длинной таблицы над продолжением по ГОСТ требуется надпись
// «Продолжение таблицы X»; modern-g7-32 не добавляет её автоматически,
// поэтому при реальном многостраничном переносе такую таблицу нужно
// редакторски разделить на части и подписать продолжение вручную.
#figure(
  table(
    columns: (42%, 58%),
    stroke: 0.5pt,
    inset: (x: 4pt, y: 4pt),
    align: (left, left),
    table.header(
      [#align(center)[Характеристика]],
      [#align(center)[Значение]],
    ),
    [Процессор], [AMD Ryzen 5 5600, 6 ядер, 12 потоков],
    [Кэш процессора], [L1d — 192 KiB; L1i — 192 KiB; L2 — 3 MiB; L3 — 32 MiB],
    [Оперативная память], [32 GiB; в WSL доступно 15 GiB; swap 4 GiB],
    [Накопитель], [INTEL SSDPEKNU512GZ, 512 GB],
    [Графический процессор], [NVIDIA GeForce RTX 5070],
    [Compute capability], [12.0],
    [Графическая память], [12 199 MiB],
    [Разделяемая память на блок], [49 152 байт (48 KiB)],
    [Константная память], [65 536 байт (64 KiB)],
    [Регистров на блок], [65 536],
    [Максимальное число нитей в блоке], [1024],
    [Максимальный размер сетки], [2 147 483 647 × 65 535 × 65 535 блоков],
    [Число мультипроцессоров], [48],
  ),
  caption: [Характеристики аппаратного обеспечения],
) <hardware-table>

Характеристики GPU были получены с помощью `nvidia-smi` и функции
`cudaGetDeviceProperties`.

== Программное обеспечение

Использованное программное обеспечение приведено в таблице 2.

#figure(
  table(
    columns: (42%, 58%),
    stroke: 0.5pt,
    inset: (x: 4pt, y: 4pt),
    align: (left, left),
    table.header(
      [#align(center)[Компонент]],
      [#align(center)[Значение]],
    ),
    [Операционная система], [Ubuntu 26.04.1 LTS в WSL],
    [Архитектура], [x86-64],
    [CUDA Toolkit], [13.4],
    [Компилятор CUDA], [nvcc 13.4.92],
    [Драйвер NVIDIA], [616.92],
    [Среда разработки], [Visual Studio Code],
  ),
  caption: [Использованное программное обеспечение],
) <software-table>

= Метод решения

На выход необходимо получить вектор. Покоординатно сравниваем элементы исходных векторов `arr1` и `arr2`. Покоординатные максимумы записываем в соответствующие координаты в `result_arr`.

Значение каждого элемента результирующего вектора не зависит от других
элементов. Поэтому разные пары чисел можно сравнивать одновременно. На CPU они
обрабатываются по очереди в обычном цикле, на GPU работа распределяется между
множеством нитей.

Каждая нить сначала вычисляет свой индекс:

```
int idx = blockIdx.x * blockDim.x + threadIdx.x;
```

Затем вычисляется число всех запущенных нитей:

```
int offset = blockDim.x * gridDim.x;
```

После обработки одного элемента выполняется `idx += offset`. Поэтому нить
переходит к следующему элементу, который относится к ней.

Последовательный алгоритм имеет сложность $O(n)$. GPU выполняет ту же общую
работу, но распределяет её между большим числом нитей.

= Описание программы

Работа состоит из трёх исходных файлов:

- `lab1.cu` — сама лабораторная;
- `benchmark.cu` — проверка и сравнение GPU- и CPU-решения;
- `properties.cu` — вывод характеристик GPU.

Векторы хранятся в массивах типа `float`. Переменная `n` типа `int` хранит
количество элементов. Указатели с приставкой `dev_` относятся к памяти GPU,
остальные указатели — к оперативной памяти компьютера.

== CUDA-ядро

В программе реализовано одно ядро `kernel`:

```
__global__ void kernel(float *arr1, float *arr2, float *result_arr, int n) {
    int idx = blockIdx.x * blockDim.x + threadIdx.x;
    int offset = blockDim.x * gridDim.x;

    while (idx < n) {
        result_arr[idx] = arr1[idx] > arr2[idx] ? arr1[idx] : arr2[idx];
        idx += offset;
    }
}
```

== CPU-версия

В CPU-версии используется обычный цикл с условием:

```
void cpu_max(float *arr1, float *arr2, float *result, int n) {
    for (int idx = 0; idx < n; ++idx) {
        result[idx] = arr1[idx] > arr2[idx] ? arr1[idx] : arr2[idx];
    }
}
```

= Результаты

== Проведение замеров

Время работы ядра измерялось с помощью CUDA Events. Перед каждым замером ядро
один раз запускалось для прогрева. Для каждого массива выполнялось 100 повторов. В таблице 3 указано среднее
время одного запуска. Копирование данных между CPU и GPU в это время не входит.

Время CPU-версии измерялось с помощью `chrono`. Вывод данных в консоль не
измерялся. 

Все программы были собраны с уровнем оптимизации `-O2`.

== Время работы CUDA-ядра

#figure(
  table(
    columns: (27%, 24%, 24%, 26%),
    stroke: 0.5pt,
    inset: (x: 3pt, y: 4pt),
    align: (center, right, right, right),
    table.header(
      [#align(center)[Конфигурация]],
      [#align(center)[$n$ = 1 000, мс]],
      [#align(center)[$n$ = 1 000 000, мс]],
      [#align(center)[$n$ = 100 000 000, мс]],
    ),
    [#cuda-config(1, 32)], [0,013005], [4,650862], [1018,690918],
    [#cuda-config(32, 32)], [0,008221], [0,147555], [33,046387],
    [#cuda-config(64, 128)], [0,010842], [0,024081], [4,978114],
    [#cuda-config(128, 256)], [0,008022], [0,011783], [2,202348],
    [#cuda-config(256, 512)], [0,007284], [0,011443], [2,254116],
    [#cuda-config(1024, 1024)], [0,009361], [0,014301], [2,110281],
  ),
  caption: [Среднее время работы CUDA-ядра],
) <cuda-time-table>

== Сравнение с CPU

Ускорение вычислялось по формуле

$ S = T_("CPU") / T_("GPU") $

где $T_("CPU")$ — время работы CPU-версии; $T_("GPU")$ — время работы CUDA-ядра.

Результаты сравнения приведены в таблице 4.

#figure(
  table(
    columns: (16%, 16%, 16%, 34%, 18%),
    stroke: 0.5pt,
    inset: (x: 3pt, y: 4pt),
    align: (right, right, right, center, right),
    table.header(
      [#align(center)[Размер $n$]],
      [#align(center)[CPU, мс]],
      [#align(center)[GPU, мс]],
      [#align(center)[Лучшая конфигурация]],
      [#align(center)[Ускорение]],
    ),
    [1 000], [0,000122], [0,007284], [#cuda-config(256, 512)], [0,017],
    [1 000 000], [0,154780], [0,011443], [#cuda-config(256, 512)], [13,53],
    [100 000 000], [40,936608], [2,110281], [#cuda-config(1024, 1024)], [19,40],
  ),
  caption: [Сравнение времени работы CPU и GPU],
) <cpu-gpu-table>

На маленьком массиве CPU работает быстрее. Само вычисление занимает очень мало
времени, поэтому расходы на запуск CUDA-ядра оказываются больше полезной работы.
На среднем массиве GPU быстрее в 13,53 раза, а на большом — в 19,40 раза.
Cравнивается время вычисления, время копирования массивов между CPU и
GPU не учитывается.

Конфигурация #cuda-config(1, 32) плохо подходит для больших массивов, потому что
в ней запускается всего 32 нити.

Для маленького массива слишком большая конфигурация тоже
невыгодна, запуск лишних нитей не ускоряет вычисления.

= Выводы

В работе были созданы CUDA- и CPU-версии программы для решения задачи. Задача хорошо подходит для параллельной обработки,
так как все элементы результата вычисляются независимо друг от друга.

Для небольшого массива использование GPU не дало преимущества. Для массива из
1 000 000 элементов было получено ускорение в 13,53 раза, а для массива из
100 000 000 элементов — в 19,40 раза. GPU выгоднее использовать
для достаточно больших объёмов данных.
